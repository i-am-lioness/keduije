/* eslint-env mocha, browser */
import React from 'react';
import { expect, assert } from 'chai';
import { mount, shallow } from 'enzyme';
import sinon from 'sinon';
import LyricDisplay from '../react/components/lyric-display';

describe('/new_music', function () {
  it('displays original values', function () {

  });

  it('displays new value as being edited, instead of original', function () {

  });

  it('generates image results from title and artist', function () {

  });

  it('stores image that user selected', function () {

  });

  it('only saves values that changed', function () {

  });

  it('displays artwork input only for non youtube video', function () {

  });
});
