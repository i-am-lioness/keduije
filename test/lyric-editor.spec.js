/* eslint-env mocha */
import React from 'react';
import { expect } from 'chai';
import { mount, shallow } from 'enzyme';
import LyricEditor from '../react/components/lyric-editor';

describe('<LyricEditor />', () => {
  it('opens with the right time segements', () => {

  });

  it('has play button play only the segment of the video that is being edited', () => {

  });

  it('shows different dialog for editing vs creating lines ', () => {

  });

  it('allows users to chnage time markers ', () => {

  });

  it('moves tail with time mark', () => {

  });

  it('is draggable', () => {

  });
});
