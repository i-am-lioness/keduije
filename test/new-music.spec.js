/* eslint-env mocha, browser */
import React from 'react';
import { expect, assert } from 'chai';
import { mount, shallow } from 'enzyme';
import sinon from 'sinon';
import LyricDisplay from '../react/components/lyric-display';

describe('/new_music', function () {
  it('shows artwork url for audio file', function () {

  });

  it('rejects non video and non audio url source', function () {

  });

  it('redirects to slug-based url after song creation', function () {

  });

  it('saves correct title and url after song creation', function () {

  });

  it('automatically stores YT thumbnail for new video, if none selected', function () {

  });

  it('stores selected image instead of YT thumbnail', function () {

  });
});
