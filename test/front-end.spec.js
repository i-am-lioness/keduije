/* eslint-env mocha, browser */
import React from 'react';
import { expect, assert } from 'chai';
import { mount, shallow } from 'enzyme';
import sinon from 'sinon';

describe('The front end', () => {
  it('shows login button, when not logged in', (done) => {

  });

  it('allows user to login on via fb', (done) => {

  });

  it('shows avatar, when logged in', (done) => {

  });

  it('user can log out when logged in', (done) => {

  });

  it('buttons do not maintain hover state when touched', function () {

  });

  it('can scroll up on touch screen without triggering click event', function () {
    // https://github.com/nnennaude/keduije/commit/8560a881e364160177cf30ccdc7d8fb0a3ae8a8a

  });

  it('never shows footer until scroll', function () {

  });
});
